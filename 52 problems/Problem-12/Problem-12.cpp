#include<stdio.h>
int main()
{
    int T, N, i;

    scanf("%d",T);

    for(i = 1; i <= T; i++)
    {
        scanf("%d",N);

        long long int factorial = 1, i;
        for(i = 2; i <= N; i++)
        {
            factorial = factorial * i;

            int mod, zero_count;

            while(number > 0)
            {
                mod = number % 10;
                if (mod == 0) zero_count++;
                else break;
                number = number /10;
            }
        }
    }
}
